_subdirectory: template
_answers_file: .template.yaml

# ----- Questions ----- #

project_name:
  type: str
  help: What is the name of the project?

package_name:
  type: str
  default: "{{ project_name | replace('-', '_') }}"
